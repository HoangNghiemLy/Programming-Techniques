#include <stdio.h>
int main(){
	printf("***XAC DINH SO CHAN LE***");
	int a;
	printf("\nNhap so a = ");
	scanf("%d",&a);
    if (a %2 == 0)
	{printf("%d la so chan ",a); 
	 } else{
	 	printf("%d la so le ",a); 
	 } 
} 
